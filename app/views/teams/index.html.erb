<p id="notice"><%= notice %></p>

<div class="container2">

<h1>Listing Teams</h1>


<table>
  <thead>
    <tr>
      <th>Notes</th>
      <th>Score</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @teams.each do |team| %>
      <tr>
        <td><%= image_tag team.image.url(:thumb) %></td>
        <td><%= team.notes %></td>
        <td><%= team.user.email if team.user %></td>
        <td><%= link_to 'View Team', team , class: "btn btn-primary" %></td>
        <td><%= link_to 'Edit', edit_team_result_path(team), class: "btn btn-warning" %></td>
        <td><%= link_to 'Destroy', team, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Team', new_team_path, class: "btn btn-success" %>

</div>
